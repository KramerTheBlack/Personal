---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Release.Namespace }}-{{ .Chart.Name }}-notification
  labels:
    pv: {{ .Release.Namespace }}-{{ .Chart.Name }}-notification
spec:
  capacity:
    storage: 5G
  claimRef:
    name: {{ .Release.Namespace }}-{{ .Chart.Name }}-notification
    namespace: {{ .Release.Namespace }}
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs
  nfs:
    path: {{ .Values.global.nfs.kubestore.basepath}}{{ .Chart.Name }}/notification/logs
    server: {{ .Values.global.nfs.kubestore.server}}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Namespace }}-{{ .Chart.Name }}-notification
  namespace: {{ .Release.Namespace }}
  labels:
    service: {{ .Chart.Name }}-notification
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5G
  storageClassName: nfs
  selector:
    matchLabels:
      pv: {{ .Release.Namespace }}-{{ .Chart.Name }}-notification

status: {}
